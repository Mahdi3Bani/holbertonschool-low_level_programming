#include <stdio.h>
#include <main.c>
/**
 * main - using putchar function to print the alphabet
 * Return: nothing (success)
 */
void print_alphabet(void);
{
	char i;
	for (i = 'a'; c <= 'z'; ++c)
		_putchar(i);
	_putchar('\n');




}
