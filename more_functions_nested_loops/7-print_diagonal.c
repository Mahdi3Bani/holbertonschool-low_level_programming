#include "main.h"
/**
 * print_diagonal - draws a diagonal line on the terminal
 *
 * @n: global var
 */
void print_diagonal(int n)
{
int i;
	if (n > 0)
	{
		for (j = 0; j < 0; j++)
	for (i = 1; i <= j; i++)
		_putchar(' ');
	_putchar(92);
	_putchar('\n');
	}
	else
		_putchar('\n');


}
